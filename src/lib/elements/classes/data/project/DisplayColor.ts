export class DisplayColor {
    public displayName: string;
    public hex: string;
    public textColor: string;

    constructor(displayName: string, hex: string, textColor: string) {
        this.displayName = displayName;
        this.hex = hex;
        this.textColor = textColor;
    }   
}