export class RouteItem {
    public route: string;
    public icon: string;
    public name: string;

    constructor(route: string, icon: string, name: string) {
        this.route = route;
        this.icon = icon;
        this.name = name;
    }
}